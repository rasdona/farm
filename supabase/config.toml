# ============================================================
# Supabase CLI configuration — AgriConnect Nepal
# Used by GitHub Actions to deploy edge functions
# Minimal on purpose: avoids schema errors on newer CLI versions
# ============================================================
project_id = "farm"
